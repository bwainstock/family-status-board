# Secrets — never committed. Use `wrangler secret put` for production.
.dev.vars
.env
.env.*

# Node
node_modules/
dist/
.wrangler/

# PlatformIO
.pio/
.pioenvs/
.piolibdeps/

# Firmware secrets (WiFi credentials, Frame shared secret)
firmware/include/secrets.h

# OS
.DS_Store
